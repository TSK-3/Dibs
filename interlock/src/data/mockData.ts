// src/data/mockData.ts — the brand asset that is still real. Every mock dataset
// that used to live here (agents, scope matrix, wire trace) has been deleted:
// those surfaces are powered by the identity service and the live WebSocket
// backend now.
export const BRAND_LOGO_URL = '/interlock-logo.svg';
